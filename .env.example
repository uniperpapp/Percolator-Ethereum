# Copy to .env and fill in. NEVER commit .env.
# --- RPC / deploy ---
MAINNET_RPC_URL=
SEPOLIA_RPC_URL=
# Deployer key for scripts (use a throwaway / hardware signer; never a funded prod key here)
PRIVATE_KEY=
# Etherscan verification
ETHERSCAN_API_KEY=

# --- oracle endpoints (later milestones) ---
PYTH_HERMES_URL=
# Chainlink feed addresses are read on-chain (no key needed)
